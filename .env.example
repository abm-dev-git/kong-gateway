# Kong Gateway Configuration
# Copy this file to .env and fill in the values

# ===========================================
# Kong Database (PostgreSQL)
# ===========================================
KONG_DATABASE=postgres
KONG_PG_HOST=kong-db
KONG_PG_USER=kong
KONG_PG_PASSWORD=kongpassword
KONG_PG_DATABASE=kong

# ===========================================
# Backend API Connection
# ===========================================
# For Docker network (when abm.dev-platform is running):
ABMDEV_API_URL=http://abm-dev-api:8080
# For local development without Docker:
# ABMDEV_API_URL=http://localhost:8000

# ===========================================
# Clerk Authentication (JWT)
# ===========================================
# Get these from Clerk Dashboard
CLERK_JWKS_URL=https://your-clerk-domain.clerk.accounts.dev/.well-known/jwks.json
CLERK_ISSUER=https://your-clerk-domain.clerk.accounts.dev

# ===========================================
# Redis (Rate Limiting)
# ===========================================
REDIS_HOST=redis
REDIS_PORT=6379

# ===========================================
# Kong Ports (external)
# ===========================================
KONG_PROXY_PORT=8080
KONG_ADMIN_PORT=8081
KONG_PROXY_SSL_PORT=8443
